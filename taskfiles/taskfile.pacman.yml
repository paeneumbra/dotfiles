version: '3'

tasks:

  assert:
    internal: true
    cmds:
      - task: :helper:assert
        vars:
          REQUIRED_TOOLS: yay

  setup:
    desc: Install yay and pacman packages
    platforms: [ linux ]
    cmds:
#      - bash "{{.ROOT_DIR}}/installation/linux/minimal-setup.sh"

  refresh:
    desc: Upgrade and update yay and pacman packages
    internal: true
    platforms: [ linux ]
    cmds:
      - yay -Syu --noconfirm

#  install:
#    desc: Install packages
#    platforms: [ linux ]
#    cmds:
#      - yay -Ss --noconfirm
